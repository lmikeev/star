
WEB_INTERFACE			= ${web-interface}

HAVE_GNUPLOT			= ${GNUPLOT_FOUND}
HAVE_LIBSBML			= ${LIBSBML_FOUND}
HAVE_MCR				= ${MCR_FOUND}
HAVE_NLOPT				= ${NLOPT_FOUND}

C_COMPILER 				= ${CMAKE_C_COMPILER}
CXX_COMPILER			= ${CMAKE_CXX_COMPILER}

C_FLAGS 				+= ${CMAKE_C_FLAGS}
CXX_FLAGS 				+= ${CMAKE_CXX_FLAGS}
SHARED_LINKER_FLAGS		+= ${CMAKE_SHARED_LINKER_FLAGS}

BOOST_INCLUDE_DIR		= ${Boost_INCLUDE_DIR}
BOOST_LIBRARIES			= ${Boost_FILESYSTEM_LIBRARY} ${Boost_PROGRAM_OPTIONS_LIBRARY} ${Boost_SYSTEM_LIBRARY}

LUA_INCLUDE_DIR			= ${LUA_INCLUDE_DIR}

LIBSBML_INCLUDE_DIR 	= ${LIBSBML_INCLUDE_DIR}
LIBSBML_LIBRARY			= ${LIBSBML_LIBRARY}

LIBMYSQLCLIENT_INCLUDE_DIR = ${LIBMYSQLCLIENT_INCLUDE_DIR}
LIBMYSQLCLIENT_LIBRARY	= ${LIBMYSQLCLIENT_LIBRARY}

MCC						= ${MCC}
MCR_INCLUDE_DIR			= ${MCR_INCLUDE_DIR}
MCR_LIBRARY				= ${MCR_LIBRARY}
LIBMATLABSD_LIBRARY		= ${LIBMATLABSD_LIBRARY}

NLOPT_INCLUDE_DIR 		= ${NLOPT_INCLUDE_DIR}
NLOPT_LIBRARY			= ${NLOPT_LIBRARY}

BINARY_DIR				= ${star_BINARY_DIR}
PLATFORM_DIR			= ${PLATFORM_DIR}
